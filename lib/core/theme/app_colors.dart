// Centralized color constants for consistent theming across the app.
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Component backgrounds
  static const appBarBackground = Colors.white;
  static const scaffoldBackground = Color(0xFFF4F6F8);
  static const bottomNavBackground = Colors.white;

  // Brand colors
  static const primary = Color(0xFFEACE6A);
  static const secondary = Color(0xFF004E64);
  static const danger = Color(0xFFEF476F);

  // Text colors
  static const onSurface = Color(0xFF1A2C32);
  static const textMuted = Color(0xFF64748B);
  static const navLabelColor = Color(0xFF94A3B8);

  // Borders and surfaces
  static const divider = Color(0xFFE2E8F0);
  static const shadow = Color(0x14004E64);
}
