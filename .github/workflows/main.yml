name: CI/CD Pipeline (Tests & Quality Checks)

# Runs tests, analysis, and quality checks on pull requests
# For builds, see build-verification.yml (manual or on-demand)
on:
  pull_request:
    branches: [ main, develop ]

jobs:
  # Job 1: Static Analysis and Testing
  analyze_and_test:
    name: 📊 Analyze & Test
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository code
      - name: 📥 Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # Step 2: Setup Flutter environment
      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@f2c4f6686ca8e8d6e6d0f28410eeef506ed66aff  # v2.18.0
        with:
          flutter-version: '3.32.6'
          channel: 'stable'
          cache: true

      # Step 3: Verify Flutter installation
      - name: 🔍 Verify Flutter Installation
        run: flutter --version

      # Step 4: Get Flutter dependencies
      - name: 📦 Get Dependencies
        run: flutter pub get

      # Step 5: Generate Mock Firebase Configs (for CI)
      - name: 🔧 Generate Mock Firebase Configs
        run: dart run tools/generate_mock_firebase_configs.dart

      # Step 6: Verify that dependencies resolved properly
      - name: 🔍 Verify Dependencies
        run: flutter pub deps

      # Step 7: Run static code analysis — fails on any warning or error
      - name: 📊 Run Static Analysis
        run: flutter analyze

      # Step 8: Check code formatting
      - name: 🎨 Check Code Formatting
        run: |
          echo "Checking code formatting..."
          dart format --set-exit-if-changed . || {
            echo "⚠️ Code formatting issues found"
            echo "Run 'dart format .' to fix formatting"
            echo "For now, this is informational only"
          }

      # Step 9: Run unit and widget tests (exclude integration tests)
      - name: 🧪 Run Tests
        run: flutter test --coverage --reporter=github --exclude-tags=integration test/unit/ test/widget/

      # Step 10: Upload coverage reports
      - name: 📈 Upload Coverage to Codecov
        uses: codecov/codecov-action@ab904c41d6ece82784817410c45d8b8c02684457  # v3.1.6
        if: success()
        with:
          file: coverage/lcov.info
          flags: flutter
          name: PlayWithMe Coverage
          fail_ci_if_error: false

  # Job 2: Cloud Functions Tests (TypeScript)
  cloud_functions_tests:
    name: ☁️ Cloud Functions Tests (TypeScript)
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository code
      - name: 📥 Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # Step 2: Setup Node.js environment
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@1d0ff469b7ec7b3cb9d8673fde0c81c44821de2a  # v4.2.0
        with:
          node-version: '22'

      # Step 3: Install dependencies
      - name: 📦 Install Dependencies
        working-directory: functions
        run: npm ci

      # Step 4: Run TypeScript compilation check
      - name: 🔍 Check TypeScript Compilation
        working-directory: functions
        run: npm run build

      # Step 5: Run unit tests
      - name: 🧪 Run Cloud Functions Tests
        working-directory: functions
        run: npm test

      # Step 6: Audit production dependencies for high/critical CVEs
      # --omit=dev excludes dev-only tooling (ESLint, Jest, etc.) that never ships to production
      # --audit-level=high fails only on high or critical severity vulnerabilities
      - name: 🔒 Audit npm Production Dependencies
        working-directory: functions
        run: npm audit --audit-level=high --omit=dev

  # Job 3: Python Cloud Functions Tests
  python_functions_tests:
    name: 🐍 Python Cloud Functions Tests
    runs-on: ubuntu-latest
    # Only run if Python files changed
    if: |
      github.event_name == 'pull_request' && (
        contains(github.event.pull_request.changed_files, 'functions/python/') ||
        github.event.pull_request.changed_files == null
      )

    steps:
      # Step 1: Checkout the repository code
      - name: 📥 Checkout Repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0

      # Step 2: Check if Python files changed
      - name: 🔍 Check for Python Changes
        id: python_changes
        run: |
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -q "^functions/python/"; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Python files changed, running tests..."
          else
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No Python files changed, skipping tests..."
          fi

      # Step 3: Setup Python environment
      - name: 🔧 Setup Python
        if: steps.python_changes.outputs.changed == 'true'
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38  # v5.4.0
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: functions/python/requirements.txt

      # Step 4: Install dependencies
      - name: 📦 Install Dependencies
        if: steps.python_changes.outputs.changed == 'true'
        working-directory: functions/python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 5: Run Python tests with coverage
      - name: 🧪 Run Python Tests
        if: steps.python_changes.outputs.changed == 'true'
        working-directory: functions/python
        run: |
          python -m pytest tests/ -v --cov=rating --cov=shared --cov-report=xml --cov-report=term-missing

      # Step 6: Upload Python coverage to Codecov
      - name: 📈 Upload Python Coverage to Codecov
        if: steps.python_changes.outputs.changed == 'true' && success()
        uses: codecov/codecov-action@ab904c41d6ece82784817410c45d8b8c02684457  # v3.1.6
        with:
          file: functions/python/coverage.xml
          flags: python-functions
          name: Python Cloud Functions Coverage
          fail_ci_if_error: false

      # Step 7: Skip message if no changes
      - name: ⏭️ Skip Message
        if: steps.python_changes.outputs.changed == 'false'
        run: echo "✅ No Python changes detected, skipping Python tests"

  # Job 4: Security and Dependency Audit
  security_audit:
    name: 🔒 Security Audit
    runs-on: ubuntu-latest
    needs: analyze_and_test # Only run if analysis and tests pass

    steps:
      # Step 1: Checkout with full history so Gitleaks can scan all commits
      - name: 📥 Checkout Repository (full history)
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          fetch-depth: 0

      # Step 2: Scan for secrets with Gitleaks
      # Scans the entire git history and working tree against 150+ secret patterns.
      # Replaces the previous manual find-based check for two filenames.
      # Allowlisted paths are configured in .gitleaks.toml (test files with
      # intentionally fake credentials).
      - name: 🔍 Scan for secrets (Gitleaks)
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7  # v2.3.9
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 3: Setup Flutter environment (for dependency audit)
      - name: 🔧 Setup Flutter
        uses: subosito/flutter-action@f2c4f6686ca8e8d6e6d0f28410eeef506ed66aff  # v2.18.0
        with:
          flutter-version: '3.32.6'
          channel: 'stable'
          cache: true

      # Step 4: Get Flutter dependencies
      - name: 📦 Get Dependencies
        run: flutter pub get

      # Step 5: Audit Dart/Flutter dependencies via OSV Scanner
      # Scans pubspec.lock against the OSV vulnerability database (supports Dart natively)
      # dart pub audit does not exist in Dart 3.x — OSV Scanner is the official alternative
      - name: 🔒 Audit Dart Dependencies (OSV Scanner)
        uses: google/osv-scanner-action/osv-scanner-action@e69cc6c86b31f1e7e23935bbe7031b50e51082de  # v2.0.2
        with:
          scan-args: |-
            --lockfile=pubspec.lock
            --format=table

  # Job 5: Final Status Check
  ci_success:
    name: ✅ CI Success
    runs-on: ubuntu-latest
    needs: [analyze_and_test, cloud_functions_tests, python_functions_tests, security_audit]
    if: always()

    steps:
      - name: 🎉 All Checks Passed
        if: |
          needs.analyze_and_test.result == 'success' &&
          needs.cloud_functions_tests.result == 'success' &&
          (needs.python_functions_tests.result == 'success' || needs.python_functions_tests.result == 'skipped') &&
          needs.security_audit.result == 'success'
        run: |
          echo "🎉 All CI/CD checks passed successfully!"
          echo "✅ Static Analysis: Passed"
          echo "✅ Flutter Tests: Passed"
          echo "✅ TypeScript Cloud Functions Tests: Passed"
          echo "🐍 Python Cloud Functions Tests: ${{ needs.python_functions_tests.result }}"
          echo "✅ Security Audit: Passed"
          echo ""
          echo "🚀 Ready for review!"
          echo ""
          echo "ℹ️ Note: Platform builds are available via build-verification.yml workflow"

      - name: ❌ CI Failed
        if: |
          needs.analyze_and_test.result != 'success' ||
          needs.cloud_functions_tests.result != 'success' ||
          (needs.python_functions_tests.result != 'success' && needs.python_functions_tests.result != 'skipped') ||
          needs.security_audit.result != 'success'
        run: |
          echo "❌ CI/CD pipeline failed!"
          echo "📊 Flutter Analysis & Tests: ${{ needs.analyze_and_test.result }}"
          echo "☁️ TypeScript Cloud Functions Tests: ${{ needs.cloud_functions_tests.result }}"
          echo "🐍 Python Cloud Functions Tests: ${{ needs.python_functions_tests.result }}"
          echo "🔒 Security Audit: ${{ needs.security_audit.result }}"
          echo ""
          echo "🛑 Please fix the failing checks before merging."
          exit 1
